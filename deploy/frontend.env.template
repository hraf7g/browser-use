# Production frontend runtime template for `/etc/utw/frontend.env`
# These values are compiled into the frontend bundle at build time.

NODE_ENV=production
PORT=3000
HOSTNAME=127.0.0.1
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_AUTH_COOKIE_NAME=utw_access_token
UTW_OPERATOR_API_KEY=CHANGE_ME_OPERATOR_API_KEY
